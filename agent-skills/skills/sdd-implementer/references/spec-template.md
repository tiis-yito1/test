# Spec: {タスク名}

> 保存先: `tasks/{作業内容}/spec.md`
> 責務: このタスクの「何を実現し、どう実装するか」を定義する実装仕様書。Phase 2 (TDD Execution) の唯一の設計リファレンスとして使用する。

## 1. 概要

- **対象機能**: {機能名} (`docs/03_features/{FeatureName}/仕様.md` 参照)
- **スコープ**: {このタスクで実装する範囲の要約}
- **スコープ外 (Out of Scope)**: {このタスクでは実装しないことを明記。スコープクリープを防止する}
- **仕様参照**: {仕様書の該当セクション名・見出し}

## 2. 前提条件・依存

| 項目 | 内容 |
| :--- | :--- |
| **Tier 1 参照** | `docs/02_architecture/アーキテクチャ.md` - {該当セクション} |
| **Tier 2 参照** | `docs/03_features/{FeatureName}/仕様.md` - {該当セクション} |
| **用語集参照** | `docs/01_product/用語集.md` - {このタスクで使用する主要用語} |
| **先行タスク** | {依存する他タスクがあれば記載。なければ「なし」} |

## 3. 振る舞い定義

Tier 2 仕様書から、このタスクのスコープに該当する振る舞いを抽出・詳細化する。
ここで定義したシナリオが §6 テストケースの元になる。

### 3.1 正常系

| # | シナリオ | 入力 | 期待される結果 |
| :- | :------- | :--- | :------------- |
| 1 | {シナリオ名} | {入力の概要} | {期待結果} |

### 3.2 異常系・境界値

| # | シナリオ | 入力 | 期待される結果 |
| :- | :------- | :--- | :------------- |
| 1 | {シナリオ名} | {異常入力} | {エラーコード / メッセージ} |

## 4. データモデル（関連部分の抜粋）

Tier 2 のドメインモデルから、このタスクに関連するエンティティ・プロパティ・制約を抜粋する。

```
{エンティティ名}:
  - {プロパティ}: {型} - {制約の要約}
```

## 5. 設計

### 5.1 Existing Assets（既存資産の調査結果）

再利用可能な既存コードを調査し、結果を記載する。

| 種別 | ファイルパス | 再利用内容 |
| :--- | :----------- | :--------- |
| 型定義 | {例: `src/Core/Events/Event.cs`} | {何を再利用するか} |
| Utility | | |
| Component | | |

- **調査範囲**: {どのディレクトリを調査したか}
- **結論**: {新規作成が必要か、既存を拡張するか}

### 5.2 Rationale（設計根拠）

#### 検討した選択肢

| 案 | 概要 | メリット | デメリット |
| :- | :--- | :------- | :--------- |
| A | {案の概要} | {メリット} | {デメリット} |
| B | {案の概要} | {メリット} | {デメリット} |

#### 採用した案と理由

- **採用**: {A or B}
- **理由**: {Tier 1 / Tier 2 の該当箇所への参照を含めた根拠}
- **既存パターンとの整合性**: {既存コードの実装方針に合っているか}

### 5.3 Files（作成・変更ファイル一覧）

| 操作 | ファイルパス | 概要 |
| :--- | :----------- | :--- |
| 新規 | {`dits-portalapp/backend/src/...`} | {何を実装するか} |
| 変更 | | |
| 新規(テスト) | {`dits-portalapp/backend/tests/...`} | {テスト対象} |

### 5.4 Dependencies（依存関係）

| 種別 | パッケージ / モジュール | 用途 | 備考 |
| :--- | :---------------------- | :--- | :--- |
| 内部 | {例: `Core.Events`} | {用途} | |
| 外部 | {例: `FluentValidation`} | {用途} | {既存 or 新規導入} |

- **循環参照チェック**: {問題なし / 懸念事項}
- **アーキテクチャ準拠**: {レイヤー違反がないか確認}

### 5.5 Interfaces（主要インターフェース）

対象スタックの言語でクラス名・メソッドシグネチャ・型定義を記載する。

```
// {言語に応じて適切なコードブロックを使用}
```

## 6. テストケース

§3 の振る舞い定義から導出する。

| # | 種別 | テスト名 | 対応シナリオ (§3) | 検証内容 |
| :- | :--- | :------- | :---------------- | :------- |
| 1 | Unit | {テスト名} | 正常系 #1 | {検証内容} |
| 2 | Unit | {テスト名} | 異常系 #1 | {検証内容} |

## 7. 受け入れ基準 (Acceptance Criteria)

このタスクは、以下がすべて満たされたとき完了とみなす。Phase 3 (Verification) でこのリストを使用して検証する。

- [ ] {§3 の全シナリオに対応するテストが Pass している}
- [ ] {Tier 1 (アーキテクチャ) に違反していない}
- [ ] {リンター・フォーマッターが Pass している}
- [ ] {具体的な機能要件: 例「検索バーにキーワードを入力するとイベントが絞り込まれる」}